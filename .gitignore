node_modules/
dist/
dist-pages/
.vite/
*.zip

# Personal-use-only source material (copyright status unverified) --
# never commit the original PDFs. icon-source.jpg is fine to keep.
assets/data/
.env
.env.local
.env.*.local
_scratch/
.venv*/

# Reference material for the dashboard redesign in progress -- not yet
# decided whether these ship as real assets, so kept local-only for now.
assets/nihongo-nin-dashboard-assets/
assets/nihongo-nin-dashboard-assets-v3/
assets/nihongo-nin-transparent-icons-v1/
assets/logo-icon.png
mockups/

# Local agent configuration and notes.
.claude/settings.local.json
CLAUDE.md

# Temporary screenshots and test-run output.
artifacts/
test-results/
