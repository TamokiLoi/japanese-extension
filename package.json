{
  "name": "japanese-extension",
  "version": "0.1.0",
  "description": "JLPT N4/N3 Kanji learning extension for Chrome/Edge (Vietnamese learners)",
  "private": true,
  "type": "module",
  "scripts": {
    "data:sync": "node --experimental-strip-types scripts/copy-data.ts",
    "icons:generate": "node --experimental-strip-types scripts/generate-icons.ts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.280",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
